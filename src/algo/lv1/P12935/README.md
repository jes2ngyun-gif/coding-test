### 왜 return new int[]{-1}인가?
```java
return {-1};
```
이렇게 쓰면 안된다. 
메서드 안에서 바로 배열을 반환할 때는 **새 배열을 만든다**는 의미로 `new int[]{...}`를 써야 한다.

```java
return new int[]{-1};
```
비유하면 {-1}은 "내용물만 적은 것"이고,
new int[]{-1}은 "배열 상자까지 새로 만들어서 그 안에 -1을 넣은 것"이다.

