package algo.lv1.p120807;
// 숫자 비교하기
// 정수 `num1`과 `num2`가 매개변수로 주어진다. 두 수가 같으면 1, 다르면 -1을 return 하도록 solution 함수를 완성해라.
public class Solution {

    public int solution(int num1, int num2) {
        if(num1 == num2){
            return 1;
        } else {
            return -1;
        }
    }
}
